
scoped
加上 scoped 的 vue 文件的模板都会加上哈希自定义属性，表示这一文件，然后给里面所有选择器加上一个属性选择器选择这个哈希属性。保证对父传过来的样式，和对子的样式不作影响，只影响自己的。


深度选择器可以取消附加属性选择器，可以影响其父组件或子组件的样式。

比如：组件提供一个插槽，父组件使用这个插槽嵌入带样式的标签，这些标签的哈希值是父组件的，而如果本组件无法对这些标签进行修改，因为附带的属性选择器（比如选插槽子代）找不到这些标签包含对应的哈希。可以通过深度选择器取消附带属性选择。